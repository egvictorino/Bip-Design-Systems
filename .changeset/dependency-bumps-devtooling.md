---
---

<!--
  Vacío a propósito: prettier, happy-dom y autoprefixer son devDependencies (build/test
  tooling), y las 4 Actions de GitHub no tocan ningún paquete publicado. No hay cambio de
  comportamiento en `dist/` para ningún consumidor — este archivo solo satisface el gate
  `changeset-check`, que corre en todo PR hacia `dev` independientemente de si el cambio
  amerita una release.
-->

