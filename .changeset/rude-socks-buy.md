---
---

Add missing test coverage for the year-picker keyboard directions (`ArrowLeft`, `ArrowUp`, `Home`, `End`, `PageUp`, `PageDown`) introduced in the previous changeset. Test-only change, no package release needed.
