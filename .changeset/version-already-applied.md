---
---

<!--
  Vacío a propósito: este mismo PR ya trae el bump aplicado (ui-components@0.4.0,
  shared-utils@0.1.1) vía `pnpm exec changeset version`, que consumió los 6 changesets
  que lo motivaron y actualizó ambos CHANGELOG.md. No hace falta un changeset adicional
  para que `dev` reciba este release — este archivo solo satisface el gate
  `changeset-check`, que no distingue "ya versionado en este PR" de "sin cambios".
-->

